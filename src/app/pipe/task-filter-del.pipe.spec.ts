import { TaskFilterDelPipe } from './task-filter-del.pipe';

describe('TaskFilterDelPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskFilterDelPipe();
    expect(pipe).toBeTruthy();
  });
});
